### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  accessToken: String!
  user: User!
}

type Category {
  id: String!
  name: String!
  posts: [Post!]!
}

type Comment {
  content: String!
  id: String!
  post: Post
  user: User!
  userId: String!
}

input CreatePinInput {
  about: String!
  category: String!
  destination: String!
  image: String!
  title: String!
}

type Mutation {
  addComment(content: String!, postId: String!): Comment!
  createPin(input: CreatePinInput!): Post!
  deletePin(postId: String!): Post!
  googleLogin(tokenId: String!): AuthPayload!
  logout: User!
  refreshAuth: AuthPayload!
  savePost(postId: String!, userId: String!): SavePayload!
}

type Post {
  about: String!
  category: Category!
  comments: [Comment!]!
  destination: String!
  id: String!
  image: String!
  save: [Save!]
  title: String!
  user: User
  userId: String!
}

type Query {
  feed: [Post!]!
  getPin(postId: String!): Post!
  getUser(userId: String!): User
  me: User!
  more(category: String!, postId: String!): [Post!]!
  search(searchTerm: String!): [Post!]!
}

type Save {
  id: String!
  post: Post!
  postId: String!
  user: User!
  userId: String!
}

type SavePayload {
  message: String!
  success: Boolean!
}

type User {
  comments: [Comment!]!
  email: String!
  id: String!
  image: String!
  name: String!
  posts: [Post!]!
  save: [Save!]
}